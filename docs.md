Run .install script. Make shure it is executable:
```shell
chmod +x .install.sh
```



laravel
curl -s "https://laravel.build/example-app?with=mysql,redis" | bash
composer require laravel/breeze --dev
php artisan breeze:install
php artisan migrate
npm install
npm run dev
sail artisan lang:publish

